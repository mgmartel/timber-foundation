{#

Sidebar right is exactly the layout that the parent sidebar template gives us,
so this template is empty. We could explicitly specify the classes, so that it's
easier for child themes to build on this template, but for now let's just enjoy
the silence.

#}
{% extends "layouts/_sidebar.twig" %}